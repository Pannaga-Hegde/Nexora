/**
 * ThemeToggle — removed.
 * Dark mode has been eliminated from the authenticated workspace.
 * This stub is kept to prevent build errors from any residual imports
 * while those are being cleaned up.
 */
export default function ThemeToggle() {
  return null;
}
